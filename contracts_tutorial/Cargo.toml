[package]
name = "contracts_tutorial"
version = "0.1.0"
edition = "2024"

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[package.metadata.kani]
unstable = { function-contracts = true }
